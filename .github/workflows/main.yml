on: [workflow_dispatch]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: henrik-ch/hello-world-javascript-action@v1.3
        with:
          who-to-greet: 'Henrik'
          limit-price: '10.02'
          current-price: '12.03'
          previous-price: '8.04'
      # Use the output from the `hello` step
      - name: Get the output time
        run: |-
          echo "The time was ${{ steps.hello.outputs.time }}"
          echo "The limit event was ${{ steps.hello.outputs.limit-event }}"
